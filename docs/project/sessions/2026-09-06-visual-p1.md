# 2026-09-06 — V1 visual: batch P1 (F1+F2) su `feat/visual-replatform`

> Override del vincolo "dopo R3" confermato dall'utente ("procedi"): batch limitato a
> modifiche class-level senza logica. F3/F4/F5 richiedono decisioni prodotto (aperte).

## Fatto

- **F1**: canvas pagine `bg-black` (#000) → `bg-[var(--background)]` (#0a0a0a) in 26 punti:
  LayoutShell, ListingShell, LoadingFallback, home, shop/loading, cart, checkout,
  checkout/success, account ×3, guide ×4, info ×6, espansioni ×2, PDP, error, not-found.
  Lasciati intenzionalmente neri: input, badge/box hero, chrome (Header/Footer/MobileMenu/
  StickyAddToCart), overlay `bg-black/40`, spedizione selezionata, info-box checkout.
- **F2**: regola globale `:focus-visible` (outline 2px accent giallo, offset 2px) in
  `globals.css` per `a, button, input, select, [role=button]` — niente al click mouse,
  visibile solo da tastiera. Input/select mantengono anche il bordo accent su `:focus`.

## Verifica

- `tsc --noEmit` 0 errori · `pnpm test` 52/52 ✓ · `next build` ok (31 pagine).
- Commit su `feat/visual-replatform` (NON su main: main resta baseline per E2E R3).

## Aperto (decisioni utente) — RISOLTE 2026-09-07

- F3 → **rimuovi link**: tolto bottone "Preordini" (Hero), voce "In Attesa" (Footer),
  righe Preordini (llms.txt/llms-full.txt), branch breadcrumb `/shop/preorders` (ClientListing).
  Zero riferimenti a `shop/preorders` in `src/`.
- F4 → **rimuovi sezione**: `CartSocialProof` tolto da home + file cancellato (numeri fake spariti).
- F5 → **rimanda**: placeholder footer invariati, da fare prima del go-live vendite.
- **Verifica F3+F4**: tsc 0 · test 52/52 · `next build` ok (31 pagine).

## Resta

Batch P2/P3 estetici, pilota ProductCard+HeroSection, E2E R3 su live.
